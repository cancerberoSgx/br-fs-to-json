{
  "name": "br-fs-to-json",
  "version": "0.0.2",
  "main": "src/index.js",
  "description": "Browserify transformation to embed files (globs) as JSON as JSON variables",
  "scripts": {
    "build": "tsc",
    "prepare": "npm run build", 
    "test": "jasmine"
  },
  "keywords": [
    "JavaScript",
    "browserify",
    "browserify transformation"
  ],
  "author": "Sebastian Gurin",
  "license": "MIT",
  "dependencies": {
    "fs-to-json": "0.1.3",
    "static-module": "^3.0.0"
  },
  "devDependencies": {
    "@types/jasmine": "^2.8.8",
    "@types/node": "^10.5.1",
    "@types/shelljs": "^0.8.0",
    "browserify": "^16.2.2",
    "jasmine": "^3.1.0",
    "shelljs": "^0.8.2"
  }
}
